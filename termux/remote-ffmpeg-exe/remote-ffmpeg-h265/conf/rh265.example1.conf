# 这是一台名为  example1  的机器的配置, 如需增加新机，仅需复制此文件，把文件名中缀的example1改成别的名称，再修改文件内容中的具体参数即可

HOST='服务器地址'
PORT='端口'
USER='用户'
PASSWD='密码'

REMOTEDIR="~/tmp/termux-ffmpeg-remote"    #注意，为了照顾使用特殊目录(~)，故不要使用含有空格目录
REMOTE_TMPFILE=`date +%Y%m%d-%H%M%S`-$((RANDOM%10000))
