whoami
#write to .bashrc
# if no openssh then install it
# if no golang then install it
# if no make then install it
# sshd
# username=`whoami`
# frpc -c frpc.ini
# report {username, sshport, frpc_port, frpc_secret} to Server
